variables:
  - name: Remote
    description: Testing
    type: bool
    default: false

skip_files:
  - path: README.md
  - path: generate_txtar.sh
  - path: boilerplate.txtar
    if: "{{ not .Remote }}"
