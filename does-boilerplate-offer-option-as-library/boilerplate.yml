variables:
  - name: Remote
    description: Testing
    type: boolean
    default: true

skip_files:
  - path: README.md
  - path: generate_txtar.sh
  - path: boilerplate.txtar
    if: "{{ not .Remote }}"
