set shell := ["bash", "-uec"]

[group('maint')]
default:
    @just --list

[group('maint')]
pre-commit:
    pre-commit install --config .pre-commit-config.yaml

[group('maint')]
fmt:
    pre-commit run --all-files
